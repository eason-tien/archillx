global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/alert_rules.yml

scrape_configs:
  - job_name: archillx
    static_configs:
      - targets: ["archillx:8000"]
    metrics_path: /v1/metrics
